import 'package:flutter/material.dart';

class CuentaConfigScreen extends StatelessWidget {
  const CuentaConfigScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('CuentaConfiScreen'),
    );
  }
}
